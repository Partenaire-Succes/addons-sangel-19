<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_migration_wizard_form" model="ir.ui.view">
        <field name="name">x3.migration.wizard.form</field>
        <field name="model">x3.migration.wizard</field>
        <field name="arch" type="xml">
            <form string="Migration Sage X3 → Odoo">
                <sheet>
                    <h2>Migration Sage X3 → Odoo</h2>

                    <!-- Config & Mode -->
                    <group>
                        <group string="⚙️ Paramètres">
                            <field name="config_id"/>
                            <field name="mode" widget="radio"/>
                            <field name="update_existing"/>
                        </group>
                        <group string="📅 Période (écritures/factures)">
                            <field name="date_debut"/>
                            <field name="date_fin"/>
                        </group>
                    </group>

                    <!-- Sélection des objets -->
                    <group string="📦 Objets à migrer">
                        <group>
                            <field name="migrate_plan_comptable"/>
                            <field name="migrate_clients"/>
                            <field name="migrate_fournisseurs"/>
                        </group>
                        <group>
                            <field name="migrate_ecritures"/>
                            <field name="migrate_factures_ventes"/>
                            <field name="migrate_factures_achats"/>
                        </group>
                    </group>

                    <!-- Résultats -->
                    <group string="📊 Résultats"
                           attrs="{'invisible': [('state', '=', 'draft')]}">
                        <field name="result_summary" readonly="1" nolabel="1"
                               widget="text"/>
                    </group>
                </sheet>

                <footer>
                    <button name="action_run"
                            string="▶ Lancer"
                            type="object"
                            class="btn-primary"
                            attrs="{'invisible': [('state', '=', 'done')]}"/>
                    <button string="Fermer"
                            class="btn-secondary"
                            special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_migration_wizard" model="ir.actions.act_window">
        <field name="name">Lancer une migration</field>
        <field name="res_model">x3.migration.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
