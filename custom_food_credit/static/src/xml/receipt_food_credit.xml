<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="custom_food_credit.OrderReceiptFoodCredit"
       t-inherit="point_of_sale.OrderReceipt"
       t-inherit-mode="extension">
        <xpath expr="//div[hasclass('before-footer')]" position="before">
            <t t-if="order.partner_id">
                <t t-set="foodCreditPayment"
                   t-value="paymentLines.find(line => line.payment_method_id.is_food)"/>
                <t t-set="foodCreditPaid"
                   t-value="foodCreditPayment ? foodCreditPayment.getAmount() : 0"/>
                <t t-set="remainingBalance"
                   t-value="(order.partner_id.food_credit_balance || 0) - foodCreditPaid"/>
                <div class="pos-receipt-food-credit-balance text-start pt-2"
                     style="font-size: 90%; border-top: 1px dashed #777; margin-top: 8px; padding-top: 8px;">
                    <div class="fw-bold mb-1" style="font-size: 102%;">
                        &#9651; Crédit alimentaire
                    </div>
                    <div class="d-flex justify-content-between">
                        <span>Solde restant :</span>
                        <span t-out="formatCurrency(remainingBalance)"
                              class="font-monospace fw-bold"/>
                    </div>
                </div>
            </t>
        </xpath>
    </t>
</templates>
